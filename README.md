1. Create a basic react/nextjs application✅
2. Connect our wallet, with a nicer application✅
3. Implement this function✅
```javascript
function airdropERC20(
    address tokenAddress,
    address[] calldata recipients,
    uint256[] calldata amounts,
    uint256 totalAmount
)
```
1. e2e testing✅
    1. When we connect, we see the form
    2. When disconnected, we don't
2. do challenges
    1. Make the inputs stay when page refreshes
    2. create transaction details info
    3. Make the button have a spinning icon when confirming in wallet. and when confirmed sending transaction.
3. Deploy to IPFS
